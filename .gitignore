# Dependencies — huge, and regenerated by `npm install` on any machine
node_modules/

# Next.js build output — regenerated by `npm run build`
.next/
out/

# Environment variables — will hold secrets (Supabase keys, Paystack keys) later; never commit these
.env
.env.local
.env.production

# OS/editor junk
.DS_Store
*.log
